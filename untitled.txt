ROBUX Generator
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Fake Robux Generator — PRANK</title>
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <style>
    :root{font-family:Inter,system-ui,Segoe UI,Roboto,Arial,sans-serif}
    *{box-sizing:border-box}
    body{margin:0;background:linear-gradient(180deg,#071029,#071322);color:#e6eef8;display:flex;align-items:center;justify-content:center;min-height:100vh;padding:24px}
    .card{width:430px;max-width:100%;background:linear-gradient(180deg,rgba(255,255,255,0.02),rgba(255,255,255,0.01));border-radius:14px;padding:20px;box-shadow:0 10px 40px rgba(2,6,23,0.65);border:1px solid rgba(255,255,255,0.03)}
    h1{margin:0;font-size:20px;text-align:center}
    p.lead{font-size:13px;opacity:0.85;text-align:center;margin:8px 0 18px}
    label{display:block;font-size:12px;color:#b6c6d8;margin:10px 0 6px}
    input, select{width:100%;padding:10px;border-radius:10px;border:1px solid rgba(255,255,255,0.06);background:rgba(255,255,255,0.02);color:#fff;outline:none}
    .row{display:flex;gap:8px}
    .btn{width:100%;padding:12px;border-radius:12px;border:none;background:linear-gradient(90deg,#6ee7b7,#3b82f6);color:#052227;font-weight:700;cursor:pointer;margin-top:14px}
    .small{font-size:11px;color:#cbd5e1;text-align:center;margin-top:8px}
    .progress{height:14px;background:rgba(255,255,255,0.03);border-radius:999px;overflow:hidden;margin-top:12px}
    .progress > i{display:block;height:100%;width:0%;background:linear-gradient(90deg,#ffd166,#ff6b6b);transition:width 0.25s}
    .result{margin-top:14px;padding:12px;border-radius:10px;background:rgba(255,255,255,0.02);text-align:center}
    .prank-banner{margin-top:12px;padding:10px;border-radius:10px;background:#ff2d2d;color:white;font-weight:800;text-align:center}
    .confetti{position:fixed;left:0;top:0;pointer-events:none;width:100%;height:100%;overflow:hidden}
    .confetti .piece{position:absolute;width:10px;height:14px;opacity:0;transform:translateY(-20px) rotate(0deg);animation:fall 2.6s linear infinite}
    @keyframes fall{0%{opacity:1;transform:translateY(-40vh) rotate(0deg)}100%{opacity:1;transform:translateY(110vh) rotate(360deg)}}
    footer{margin-top:14px;font-size:12px;color:#e6eef8;opacity:0.85;text-align:center}
    .disclaimer{font-size:12px;color:#fff;background:rgba(255,255,255,0.03);padding:8px;border-radius:8px;margin-top:12px}
    .share{display:flex;gap:8px;margin-top:10px}
    .ghost{background:transparent;border:1px solid rgba(255,255,255,0.06);color:#dbeafe;padding:10px;border-radius:10px;cursor:pointer}
  </style>
</head>
<body>
  <div class="card" role="main">
    <h1>Fake Robux Generator — <span style="color:#ffd166">Fun Prank</span></h1>
    <p class="lead">A safe, offline prank page — it <strong>does not</strong> and <strong>will not</strong> interact with Roblox or give real Robux. The PRANK banner is unmissable.</p><label for="user">Friend's username (for show)</label>
<input id="user" placeholder="e.g. sister123" autocomplete="off" />

<label for="amount">Amount to "generate"</label>
<select id="amount">
  <option value="100">100 Robux</option>
  <option value="500">500 Robux</option>
  <option value="1000">1,000 Robux</option>
  <option value="5000">5,000 Robux</option>
  <option value="10000">10,000 Robux</option>
  <option value="999999">999,999 Robux (silly)</option>
</select>

<label style="margin-top:8px"><input type="checkbox" id="dramatic" /> <span style="vertical-align:middle">Enable dramatic progress</span></label>

<button id="go" class="btn">Start Fake Generation</button>

<div class="progress" aria-hidden="true" style="display:none">
  <i id="bar"></i>
</div>

<div id="status" class="result" style="display:none">Ready...</div>

<div id="banner" class="prank-banner" style="display:none">PRANK — THIS IS NOT REAL ROBUX</div>

<div class="disclaimer">Important: Use this only to joke with friends. Don't post it to trick strangers or claim real currency — that could be harmful.</div>

<div class="share">
  <button id="copyLink" class="ghost" title="Copy page HTML">Copy HTML</button>
  <button id="download" class="ghost" title="Download HTML file">Download .html</button>
</div>

<footer>Made for laughs — remember to reveal the prank 🙂</footer>

  </div>  <div class="confetti" aria-hidden="true" id="confetti"></div>  <script>
    // Strictly front-end, offline prank.
    const go = document.getElementById('go');
    const bar = document.getElementById('bar');
    const progress = document.querySelector('.progress');
    const status = document.getElementById('status');
    const banner = document.getElementById('banner');
    const confettiRoot = document.getElementById('confetti');
    const copyBtn = document.getElementById('copyLink');
    const downloadBtn = document.getElementById('download');

    function makeConfetti(){
      confettiRoot.innerHTML = '';
      for(let i=0;i<28;i++){
        const p = document.createElement('div');
        p.className='piece';
        p.style.left = Math.random()*100+'%';
        p.style.animationDelay = (Math.random()*1.2)+'s';
        p.style.background = ['#ffd166','#06d6a0','#118ab2','#ef476f','#a78bfa'][Math.floor(Math.random()*5)];
        p.style.transform = `rotate(${Math.random()*360}deg)`;
        p.style.width = (6 + Math.random()*12) + 'px';
        p.style.height = (10 + Math.random()*18) + 'px';
        confettiRoot.appendChild(p);
      }
    }

    go.addEventListener('click',()=>{
      // reset
      bar.style.width='0%';
      progress.style.display='block';
      status.style.display='block';
      status.textContent='Preparing prank...';
      banner.style.display='none';

      const dramatic = document.getElementById('dramatic').checked;
      let pct = 0;
      makeConfetti();
      const messages = dramatic ? [
        'Connecting to imaginary servers...',
        'Synthesizing virtual credits...',
        'Applying glittery taxes (jk)...',
        'Finalizing fake deposit...',
        'Almost there...'
      ] : [
        'Preparing...',
        'Processing...',
        'Finishing...'
      ];

      let stage = 0;
      const totalStages = messages.length;
      const interval = setInterval(()=>{
        const jump = dramatic ? (Math.floor(Math.random()*12)+6) : (Math.floor(Math.random()*20)+15);
        pct += jump;
        if(pct>100) pct=100;
        bar.style.width = pct + '%';
        status.textContent = messages[stage] + ' ('+pct+'%)';
        if(pct >= (Math.round((stage+1)/totalStages*100))){
          stage = Math.min(stage+1,totalStages-1);
        }
        if(pct>=100){
          clearInterval(interval);
          const amount = document.getElementById('amount').value;
          const user = escapeHtml(document.getElementById('user').value || 'username');
          status.innerHTML = `<strong>Done — ${amount} \"Robux\" "added" to ${user}</strong>`;
          banner.style.display='block';
          makeConfetti();

          setTimeout(()=>{
            // obvious reveal modal
            const modal = document.createElement('div');
            modal.style.position='fixed';modal.style.left='50%';modal.style.top='50%';modal.style.transform='translate(-50%,-50%)';
            modal.style.background='linear-gradient(90deg,#08101a,#0b1a25)';modal.style.padding='18px';modal.style.borderRadius='12px';modal.style.boxShadow='0 12px 50px rgba(0,0,0,0.6)';modal.style.zIndex=9999;color:'#fff';modal.style.textAlign='center';
            modal.innerHTML = `<h2 style="margin:0 0 8px;">🎉 PRANK REVEAL 🎉</h2><p style="margin:0 0 12px;">This page is a harmless prank — it does not provide real Robux or touch Roblox accounts.</p><div style="display:flex;gap:8px;justify-content:center"><button id='closePrank' style='padding:10px 14px;border-radius:10px;border:none;background:#06d6a0;color:#062d1f;font-weight:700;cursor:pointer'>Cool</button><button id='sharePrank' style='padding:10px 14px;border-radius:10px;border:none;background:#ffd166;color:#3b1f00;font-weight:700;cursor:pointer'>Show sibling</button></div>`;
            document.body.appendChild(modal);
            document.getElementById('closePrank').addEventListener('click',()=>{modal.remove();});
            document.getElementById('sharePrank').addEventListener('click',()=>{alert('Show this to your sister and then tell her it\'s a prank!');});
          },600);
        }
      }, dramatic ? 500 : 350);
    });

    // helpers
    function escapeHtml(s){
      return String(s).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;');
    }

    // Copy HTML to clipboard (page source)
    copyBtn.addEventListener('click',()=>{
      const html = document.documentElement.outerHTML;
      navigator.clipboard.writeText(html).then(()=>{copyBtn.textContent='Copied!'; setTimeout(()=>copyBtn.textContent='Copy HTML',1200);}).catch(()=>{copyBtn.textContent='Unavailable'; setTimeout(()=>copyBtn.textContent='Copy HTML',1200);});
    });

    // Download as .html file
    downloadBtn.addEventListener('click',()=>{
      const html = '<!doctype html>
' + document.documentElement.outerHTML;
      const blob = new Blob([html],{type:'text/html'});
      const url = URL.createObjectURL(blob);
      const a = document.createElement('a');
      a.href = url; a.download = 'fake-robux-prank.html'; document.body.appendChild(a); a.click(); a.remove(); setTimeout(()=>URL.revokeObjectURL(url),2000);
    });
  </script></body>
</html>